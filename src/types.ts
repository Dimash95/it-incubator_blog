export type BlogType = {
  id: number;
  name: string;
  description: string;
  websiteUrl: string;
};

export type PostBlogType = {
  name: string;
  description: string;
  websiteUrl: string;
};

export type PutBlogType = {
  name: string;
  description: string;
  websiteUrl: string;
};
